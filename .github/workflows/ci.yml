name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # The family's shared CI core (sutra.mk's sibling layer — see
  # docs/BOOTSTRAP.md). Pinned to a commit SHA, not @main and not a tag,
  # per pill-ci.yml's own header: sutra cuts no tagged release yet, and a
  # floating @main ref would move every adopting pill's CI the instant this
  # file changes, with no per-pill review step.
  #
  # 0.11.1 (msg 2783) folded the raw shellcheck step in here too, via
  # shellcheck-files — was a sibling-job step below, deleted now that
  # pill-ci.yml carries it as a real, parameterized step.
  shared:
    uses: asuramaya/sutra/.github/workflows/pill-ci.yml@149b553b75467813748165401706ce6b50ca44d6
    with:
      python-files: "src/bin/ramsteind src/bin/ramstein src/bin/ramstein-healthcheck src/bin/ramstein-update src/share/ramstein/lib/sutra.py src/share/ramstein/lib/sutra_update.py src/share/ramstein/lib/sutra_xen.py"
      shell-files: "install.sh uninstall.sh packaging/release-signing/sync-signers.sh tests/smoke.sh tests/test_signing.sh"
      shellcheck-files: "install.sh uninstall.sh packaging/release-signing/sync-signers.sh tests/smoke.sh tests/test_signing.sh"
      has-extension: true
      extension-js: "src/extension/ramstein@asuramaya/extension.js"
      extension-metadata: "src/extension/ramstein@asuramaya/metadata.json"
      man-pages: "src/data/man/man1/ramstein.1 src/data/man/man8/ramsteind.8"
      # run-attack defaults to false in pill-ci.yml; the original hand-written
      # ci.yml always ran the adversarial fuzz pass and the very first pilot
      # adoption (commit 7e6c637) silently dropped it by never setting this --
      # missed for two full report cycles since `make attack` still passed
      # locally every time, just never ran in CI. Caught only by reading the
      # ACTUAL step list off a real run (gh api .../jobs/<id> steps[].status),
      # not by the green checkmark alone.
      run-attack: true
      # RAMstein has no per-file VERSION constant to check: Pass 2 (RS-STD-1)
      # deliberately removed the old per-binary literal in favor of a single
      # packaging/VERSION source of truth, read live at runtime — the exact
      # opposite of the per-file-version-constant convention check-version.sh
      # enforces upstream, and check-repo's own "no literal version string
      # outside packaging/VERSION" rule holds the line on it. This is also
      # now pill-ci.yml's own default (msg 2783) — kept explicit here so the
      # reason stays on record rather than riding on an upstream default.
      run-check-version: false
      # run-signing-verify stays at its default (false): test_signing.sh is
      # already covered by run-smoke — make smoke's own recipe runs it
      # (DM #2913) — running it a second time here would be redundant.

  # Genuinely pill-specific, per pill-ci.yml's own header ("stays as a
  # sibling job in the calling pill's own ci.yml"). pill.js's own
  # integrity/freshness folded into check-sutra itself at 0.11.1
  # (SUTRA_EXT_DIR, set in the Makefile) and shellcheck moved into the
  # shared job above, so this now covers exactly one thing sutra.mk still
  # can't: the checkout-run resolution check across all four binaries that
  # carry the bootstrap preamble. sutra.mk's check-vendored-path validates
  # one SUTRA_CHECK_BIN per invocation; check-vendored-path-all (native as
  # of 0.11.1, driven by the Makefile's SUTRA_CHECK_BINS list) loops it.
  ramstein-specific:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Vendored-path resolution (all four binaries)
        run: make check-vendored-path-all

  # Found 2026-08-02 (alfred, diffing the published v0.11.1 .deb against its
  # own unit files): every pill in this family ships a hardened systemd
  # unit and none had ever actually STARTED one in a test — CI always ran
  # the raw binary directly. RAMstein's .deb shipped ExecStart=/usr/local/
  # bin/... (install.sh's prefix) while installing binaries to /usr/bin
  # (fixed in the deb: target, this same commit): dpkg succeeded, the
  # daemon died 203/EXEC, postinst's `|| true` swallowed it silently.
  # Undetectable from a raw-binary test no matter how thorough, since the
  # raw binary never goes near the unit file — only installing the actual
  # .deb and starting the actual unit exercises that path. GitHub's
  # ubuntu-latest runners are full VMs with systemd as PID 1 (not
  # containers), unlike a sandboxed dev environment, so this can run for
  # real rather than assume the hardening/paths are correct on paper.
  # Polls NRestarts over a window rather than checking is-active once: a
  # crash loop under Restart=on-failure can read "active" between
  # failures (ByeByte's same-day fanotify SIGSYS showed exactly this).
  systemd-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Build the .deb
        run: make deb

      - name: Install under the real hardened unit
        run: sudo dpkg -i build/deb/ramstein_*_all.deb

      # Window standardized at 30s across the family (alfred, DM #3110):
      # the negative control that proved this check works (see ef6394d's
      # commit message) measured the actual failure mode -- 4 restarts
      # within 5s, 6 within 15s, against RestartSec=5 -- so 30s gives six
      # observations of a loop that announces itself in the first five
      # seconds. SCOPE, so nobody later mistakes it for total coverage:
      # this window catches IMMEDIATE-DEATH failures -- bad ExecStart,
      # seccomp SIGSYS, a missing capability -- the class actually shipped
      # three times today. It does NOT catch a daemon that runs fine for
      # two minutes and then dies on its first poll-loop tick; that is a
      # different failure needing a different detector.
      - name: Poll ActiveState/NRestarts over 30s (not a one-shot is-active)
        run: |
          # Individual -p queries, not a single comma-list --value call:
          # `systemctl show -p A,B,C,D --value` does NOT preserve the
          # requested order (it returned MainPID,NRestarts,ActiveState,
          # SubState here, not the ActiveState,SubState,NRestarts,MainPID
          # asked for) -- caught by this very step on its first real run,
          # positional `read` silently mis-assigned every field.
          for i in $(seq 1 6); do
            active="$(systemctl show ramsteind.service -p ActiveState --value)"
            substate="$(systemctl show ramsteind.service -p SubState --value)"
            restarts="$(systemctl show ramsteind.service -p NRestarts --value)"
            mainpid="$(systemctl show ramsteind.service -p MainPID --value)"
            echo "t+$((i * 5))s: active=$active sub=$substate restarts=$restarts pid=$mainpid"
            if [ "$active" != "active" ] || [ "$restarts" != "0" ]; then
              echo "FAIL: ramsteind.service is '$active/$substate' with $restarts restart(s)" >&2
              sudo journalctl -u ramsteind.service --no-pager -n 50 >&2
              exit 1
            fi
            sleep 5
          done
          echo "ramsteind.service stable: active, 0 restarts, over 30s"

      - name: Real healthcheck + real status query
        run: |
          ramstein-healthcheck
          ramstein status

      # Operator ruling relayed via alfred (DM #3098): covering only
      # ramsteind isn't enough -- ByeByte's equivalent oneshot units
      # (update/sweep) needed to actually COMPLETE under their corrected
      # paths, not just carry the right ExecStart text, to be trusted.
      # These two are Type=oneshot: `systemctl start` blocks until they
      # finish, so Result reflects the real outcome, not just that the
      # process launched. The .timer units have no ExecStart of their own
      # to break (only OnBootSec/OnUnitActiveSec) -- testing the services
      # covers their EXECUTION path, but not their own separate failure
      # mode (see the next step: a timer can fail to ever fire at all).
      - name: Run the oneshot units to completion (not just check ExecStart text)
        run: |
          for u in ramstein-autocalm.service ramstein-update.service; do
            sudo systemctl start "$u" || true
            result="$(systemctl show "$u" -p Result --value)"
            echo "$u: Result=$result"
            if [ "$result" != "success" ]; then
              echo "FAIL: $u did not complete successfully (Result=$result)" >&2
              sudo journalctl -u "$u" --no-pager -n 30 >&2
              exit 1
            fi
          done
          echo "both oneshot units completed successfully"

      # A timer has its own failure mode starting the service directly
      # cannot reach (alfred, DM #3110): a malformed OnCalendar/OnBootSec,
      # or a Unit= naming a service that doesn't exist, produces a timer
      # that loads and silently never triggers -- RAMstein's timers rely
      # on systemd's default name-matching convention (foo.timer ->
      # foo.service) rather than an explicit Unit=, which is correct today
      # and exactly the kind of implicit coupling that breaks silently the
      # day one side gets renamed. Two instant checks, no waiting for a
      # real schedule to fire: `systemd-analyze verify` catches a Unit=
      # pointing at nothing; `start` (transient, NOT `enable` -- these
      # ship disabled by family convention and must stay that way after
      # this check) + `list-timers --all` proves the schedule actually
      # resolves to a real NEXT time against the right service. Confirmed
      # locally first: list-timers --all shows NOTHING for a disabled-but-
      # loaded timer in this systemd version, so `start` before listing is
      # required, not optional -- `stop` after restores the as-shipped state.
      - name: Timer units resolve (schedule + Unit= target, not just syntax)
        run: |
          sudo systemd-analyze verify ramstein-autocalm.timer ramstein-update.timer
          sudo systemctl start ramstein-autocalm.timer ramstein-update.timer
          listing="$(systemctl list-timers --all)"
          echo "$listing"
          for t in ramstein-autocalm.timer ramstein-update.timer; do
            echo "$listing" | grep -q "$t" || {
              echo "FAIL: $t did not resolve to a scheduled entry" >&2
              sudo systemctl stop ramstein-autocalm.timer ramstein-update.timer
              exit 1
            }
          done
          sudo systemctl stop ramstein-autocalm.timer ramstein-update.timer
          echo "both timers resolve to a real schedule and the correct service"
